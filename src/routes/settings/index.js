import {h} from 'preact';
import style from './style.css';

const Settings = () => {

    return (
        <div class={style.settings}>
            <h1>Settings</h1>
            <p>coming soon...</p>
        </div>
    );
}

export default Settings;
