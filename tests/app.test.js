import {h} from 'preact';
import App from '../src/components/app';
import {render} from "enzyme";

test('check that app can startup', () => {
    //render(<App />);
    //expect(screen.getByRole("heading")).toHaveTextContent(/spokewikids/);

});